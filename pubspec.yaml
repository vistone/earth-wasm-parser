name: wasm_parser
description: A Flutter application that calls WASM modules
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  js: ^0.7.1
  flutter_web_plugins:
    sdk: flutter
  shelf: ^1.4.0
  shelf_static: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - wasm/

